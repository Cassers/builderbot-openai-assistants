version: "3.8"
services:
  app:
    build: .
    ports:
      - "3007:3007"
    volumes:
      - app-data:/app/bot_sessions
    env_file:
      - .env
    environment:
      - NODE_ENV=development

volumes:
  app-data:
